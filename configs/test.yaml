# @package _global_

defaults:
  - _self_
  - experiment: moses
  - tokenizer: sent    # Options: sent, hsent, hsent_motif

# Random seed
seed: 42

# Model configuration
model:
  checkpoint_path: null  # Required: path to model checkpoint
  is_autograph: false  # Set to true for AutoGraph checkpoints (auto-detected if false)

# Data configuration
data:
  dataset_name: moses  # 'moses' or 'qm9'
  batch_size: 32
  num_workers: 0
  num_train: 10000  # Used for reference in metrics
  num_val: 1000
  num_test: 1000
  include_hydrogens: false
  data_root: data

# Sampling configuration
sampling:
  num_samples: 100  # -1 to use all test set
  batch_size: 32
  top_k: 10
  temperature: 1.0
  max_length: 2048

# Metrics configuration
metrics:
  compute_fcd: true  # Requires moses or fcd package

# Logging configuration
logs:
  path: outputs/test/${now:%Y-%m-%d}/${now:%H-%M-%S}
